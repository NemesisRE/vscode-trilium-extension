name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  release:
    types: [published]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  typecheck:
    name: Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Cache Trilium CKEditor plugins
        uses: actions/cache@v5
        with:
          path: vendor/
          key: trilium-plugins-${{ hashFiles('scripts/download-trilium-plugins.mjs') }}
          restore-keys: |
            trilium-plugins-

      - name: Patch Trilium plugins
        run: node scripts/patch-trilium-plugins.mjs

      - name: Type check
        run: npm run compile

  test-and-build:
    name: Test & Build
    needs: typecheck
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Cache Trilium CKEditor plugins
        uses: actions/cache@v5
        with:
          path: vendor/
          key: trilium-plugins-${{ hashFiles('scripts/download-trilium-plugins.mjs') }}
          restore-keys: |
            trilium-plugins-

      - name: Patch Trilium plugins
        run: node scripts/patch-trilium-plugins.mjs

      - name: Type check
        run: npm run compile

      - name: Run unit tests
        run: npm test

      - name: Run integration tests (VS Code extension host)
        run: xvfb-run -a npm run test:integration

      - name: Build extension bundle
        run: npm run build:prod

  package:
    name: Package VSIX
    needs: test-and-build
    runs-on: ubuntu-latest
    # Only produce a VSIX on tagged releases.
    if: github.event_name == 'release'
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Cache Trilium CKEditor plugins
        uses: actions/cache@v5
        with:
          path: vendor/
          key: trilium-plugins-${{ hashFiles('scripts/download-trilium-plugins.mjs') }}
          restore-keys: |
            trilium-plugins-

      - name: Patch Trilium plugins
        run: node scripts/patch-trilium-plugins.mjs

      - name: Update package.json version to match release tag
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          npm version "$VERSION" --no-git-tag-version --allow-same-version
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}

      - name: Build extension bundle
        run: npm run build:prod

      - name: Package VSIX
        run: npx @vscode/vsce package --no-dependencies

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v7
        with:
          name: trilium-notes-vsix
          path: '*.vsix'
          if-no-files-found: error

      - name: Attach VSIX to GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "${{ github.ref_name }}" *.vsix --clobber
